# DreamFarm AGENT Rules

项目：DreamFarm
定位：Godot 4 高清像素风农场模拟经营游戏

## 开发原则
1. 只做 MVP，优先完成可玩的单机核心循环。
2. 当前阶段不做联网、不做氪金、不做复杂剧情分支。
3. 所有游戏脚本使用 Godot 4 + GDScript。
4. 代码保持模块化，优先使用 JSON 数据驱动作物、物品、角色。
5. 每次新增功能都按小步骤推进，不要一次性扩展成大系统。
6. 每次提交修改时，必须明确说明改动了哪些文件以及这些文件的职责。

## 当前核心循环
种田 -> 收获卖钱 -> 招募助手 -> 农场自动化 -> 探索矿洞 -> 获得资源 -> 升级农场

## 工程约束
1. 场景放在 `scenes/`，脚本放在 `scripts/`，数据放在 `data/`。
2. 管理器脚本优先放在 `scripts/managers/` 并通过 autoload 接入。
3. 新功能先做最小可测试版本，优先使用键盘快捷键或控制台输出来验证。
4. 改动时避免引入未使用的系统、UI 框架或复杂依赖。
5. 保存/读取数据时优先保持结构清晰、可序列化、可调试。

## 图片提示词规则
1. 当用户要求“生成图片”“生成素材提示词”“生成某个物品的图片提示词”时，必须先读取项目根目录下的 `asset_prompt_style.md`。
2. 所有图片提示词默认基于 `asset_prompt_style.md` 的风格规范生成。
3. 除非用户明确要求更换风格，否则不得偏离该模板定义的像素风、视角、配色和负面约束。
4. 如需生成树木、作物、工具、掉落物、建筑等素材，应优先复用该模板，并在其基础上补充具体物品描述。
5. 默认输出适合直接喂给图像模型的英文强约束提示词。
6. 当用户只说“生成提示词”而未指定格式时，默认输出“完整英文模板 + 具体物品描述”的可直接投喂版本，而不是简写摘要。
7. 若用户一次请求多个同类素材，应先复用同一套模板结构，再分别补充各自的物品描述，保证整组素材风格统一。
8. 若项目中已有明确参考素材，生成提示词时应主动强调与现有 DreamFarm 素材保持一致，尤其是像素密度、轮廓强度、明暗层次、体积感和配色饱和度。
9. 生成图片提示词时，除非用户明确要求，不额外扩展成场景图、宣传图、立绘或 UI 整页设计，优先生成单个透明背景游戏素材。
